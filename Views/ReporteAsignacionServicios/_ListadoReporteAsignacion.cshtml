@model List<ReporteAsignacionModel>

<div class="row mb-1 titleCustom">
    <div class="col-12">
        <h5 class="px-4"><b>Listado de Reporte de Servicios</b></h5>
    </div>
</div>
<br />
<br />
<div class="row">

    <div class="col-12 mb-4 px-4 rounded gridCustom">
        @(Html.Kendo().Grid(Model)
            .Name("Grid")
            .Columns(columns =>
            {
                columns.Bound(p => p.vehiculoCarretera).Title("Carretera").Width(90);
                columns.Bound(p => p.vehiculoTramo).Title("Tramo").Width(90);
                columns.Bound(p => p.vehiculoKm).Title("KM").Width(90);
                columns.Bound(c => c.fechaSolicitud).Title("Fecha").Width(150);
                columns.Bound(c => c.fechaLiberacion).Title("Fecha Salida").Width(150);
                columns.Bound(c => c.evento).Title("Evento").Width(90);
                columns.Bound(c => c.fullName).Title("Nombre Solicitante").Width(200);
                columns.Bound(c => c.fullcolonia).Title("Dirección").Width(200);
                columns.Bound(c => c.tipoVehiculo).Title("Tipo Vehículo").Width(90);
                columns.Bound(c => c.noEconomico).Title("Grúa").Width(90);
                columns.Bound(c => c.Alias).Title("Alias").Width(150);
                columns.Bound(c => c.oficial).Title("Oficial").Width(150);
                columns.Bound(c => c.folio).Title("Folio").Width(90);
                columns.Bound(c => c.vehiculoPension).Title("Pensión").Width(150);
                columns.Bound(c => c.Delegacion).Title("Delegacion").Width(150);
            }).HtmlAttributes(new { style = "height:350" })
            .Pageable(pager => pager.AlwaysVisible(true).Position(GridPagerPosition.Top).PageSizes(true))
            .Sortable()
            .Scrollable(scr => scr.Height(350))
            .DataSource(dataSource => dataSource
            .Ajax()
            .ServerOperation(false)
            )
            )
    </div>

</div>

<style>
    .k-grid-content {
        overflow-x: scroll;
    }
</style>